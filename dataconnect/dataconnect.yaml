specVersion: "v1"
serviceId: "ventas-mvp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ventas-mvp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
